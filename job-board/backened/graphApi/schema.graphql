type Job {
    id: ID!
    title: String!
    description: String
    company: Company!
    date: String!
}

type Company {
    id: ID!
    name: String!
    description: String
}

type Query {
    jobs: [Job!]
}