type Job {
    id: ID!
    title: String!
    description: String
    company: Company!
    date: String!
}

type Company {
    id: ID!
    name: String!
    description: String
    jobs: [Job!]!
}

type Query {
    jobs: [Job!]
    job(id: ID!): Job
    company(id: ID!): Company
}